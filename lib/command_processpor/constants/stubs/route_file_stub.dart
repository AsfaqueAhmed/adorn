part of 'stubs.dart';

String routeFileStub = '''import 'package:flutter/material.dart';

PageRoute? onGenerateRoute(settings) {
  PageRoute? route;
  switch (settings.name) {
  }
  return route;
}

PageRoute? unknownRoute(settings) {
  PageRoute? route;
  switch (settings.name) {
  }
  return route;
}''';
